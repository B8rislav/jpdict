/**
 * The product lockup. Not in the locale files on purpose — a brand mark is the same in
 * every language, and `Brand` takes each part as a prop precisely so renaming is a change
 * here and nowhere else.
 *
 * Note the deliberate mismatch: the page says 言葉ラボ / KOTOBA LAB while `meta_site_title`
 * and the JSON-LD still say "JapChin Dict". The rename is a separate, unmade decision.
 */
export const BRAND_MARK = '語';
export const BRAND_WORDMARK = '言葉ラボ';
export const BRAND_SUBTITLE = 'KOTOBA LAB';
