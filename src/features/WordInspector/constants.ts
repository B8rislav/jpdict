/** Section ids for the inspector's accordion. Shared so the container can track
 *  open state and trigger lazy loads by the same identifiers the view renders. */
export const SECTION_TRANSLATION = 'translation';
export const SECTION_GRAMMAR = 'grammar';
export const SECTION_EXAMPLES = 'examples';
